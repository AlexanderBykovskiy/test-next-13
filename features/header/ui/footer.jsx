import {Container} from "@mui/material";

export default function Header () {
    return(
        <footer>
            <Container maxWidth="lg">
            header
            </Container>
        </footer>
    )
}
